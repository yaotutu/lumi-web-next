# 提示词优化 - 快速开始

## 功能说明

系统会**自动**使用通义千问大模型优化用户输入的提示词,使生成的图片更适合3D打印。

**特点**:
- ✅ 默认开启,无需配置开关
- ✅ 优化失败自动降级,不影响业务
- ✅ 完整的日志追踪

---

## 配置步骤

### 1. 配置环境变量

在 `.env.local` 文件中添加:

```bash
# 通义千问配置
QWEN_API_KEY=sk-your-api-key-here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-max
```

### 2. 获取 API Key

1. 访问 https://bailian.console.aliyun.com/
2. 创建 API Key
3. 复制到 `QWEN_API_KEY`

### 3. 重启服务

```bash
npm run dev
```

---

## 验证效果

### 提交任务

在前端输入: `"我要一个蓝色耳朵的可爱猫咪"`

### 查看日志

终端会显示:

```
INFO: 开始优化提示词 { userInput: "我要一个蓝色耳朵的可爱猫咪" }
INFO: ✅ 提示词优化成功
INFO: 📝 原始提示词 { prompt: "我要一个蓝色耳朵的可爱猫咪" }
INFO: ✨ 优化后提示词 {
  prompt: "cute cat figurine with blue ears, solid structure, smooth surface, stable sitting pose, clear edges, symmetrical design, matte finish, plain white background, 3/4 view, no thin parts, suitable for 3D printing"
}
```

**关键信息**:
- `📝 原始提示词` - 用户输入
- `✨ 优化后提示词` - 大模型优化后的结果(实际用于生成图片)

---

## 优化示例

### 示例 1: 简单物体

**输入**: `"一只可爱的小猫"`

**优化后**:
```
cute cat figurine, solid structure, smooth surface,
stable sitting pose, clear edges, symmetrical design,
matte finish, plain white background, 3/4 view,
no thin parts, suitable for 3D printing
```

**改进**:
- ✅ 添加了"稳定坐姿"(避免倾倒)
- ✅ 添加了"清晰边缘"(便于打印)
- ✅ 添加了"无细小部件"(避免断裂)

---

### 示例 2: 复杂结构

**输入**: `"一个镂空的花瓶"`

**优化后**:
```
simple vase, solid walls, smooth curves,
wide base for stability, no hollow patterns,
thick structure, clean edges, matte surface,
plain background, front view, 3D printable design
```

**改进**:
- ❌ 移除了"镂空"(3D打印困难)
- ✅ 改为"实心墙壁"(solid walls)
- ✅ 添加了"宽底座"(稳定性)

---

## 故障排查

### 问题 1: 没有看到优化日志

**原因**: API Key 未配置或配置错误

**解决**:
1. 检查 `.env.local` 中 `QWEN_API_KEY` 是否正确
2. 确认已重启开发服务器

---

### 问题 2: 看到降级警告

```
WARN: ⚠️ 提示词优化失败,降级使用原始输入
```

**原因**:
- API Key 无效(401)
- API 限流(429)
- 网络问题

**影响**:
- 自动使用原始提示词
- 不影响图片生成

**解决**:
- 检查 API Key 是否正确
- 检查网络连接
- 等待限流解除

---

## 更多文档

详细说明请查看: `docs/PROMPT_OPTIMIZATION.md`
